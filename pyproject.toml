[tool.poetry]
name = "GameAI"
version = "0.1.0"
description = "Serpent Projects"
authors = ["Nicholas Shindler"]

[tool.poetry.dependencies]
python = "3.6"
cython = "0.26.1"

[tool.poetry.dev-dependencies]
wheel = "*"
setuptools = "*"
serpentai = "^2018.1.2"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
